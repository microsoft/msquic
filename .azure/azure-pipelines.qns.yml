#
# This pipeline executes the interop runner for all implementations, or only the
# specified client and/or server.
#

trigger:
  batch: true
  branches:
    include:
    - main
  paths:
    exclude:
    - src/plugins/*
    - docs/*
    - README.md
pr: none

name: 0.$(Date:yyyy).$(Date:MM).$(DayOfMonth).$(Rev:rr).0

parameters:
- name: build
  type: boolean
  displayName: Build docker image
  ${{ if eq(variables['Build.Reason'], 'BatchedCI') }}:
    default: true
  ${{ if ne(variables['Build.Reason'], 'BatchedCI') }}:
    default: false
- name: measurements
  type: boolean
  displayName: Run Measurements
  default: false
- name: timeout
  type: number
  displayName: Timeout (min)
  default: 15
- name: client
  type: string
  displayName: Client
  ${{ if eq(variables['Build.Reason'], 'BatchedCI') }}:
    default: 'special'
  ${{ if ne(variables['Build.Reason'], 'BatchedCI') }}:
    default: 'all'
- name: server
  type: string
  displayName: Server
  ${{ if eq(variables['Build.Reason'], 'BatchedCI') }}:
    default: 'special'
  ${{ if ne(variables['Build.Reason'], 'BatchedCI') }}:
    default: 'all'

jobs:
- ${{ if parameters.build }}:
  - job: publish_docker
    displayName: Build and Publish
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - checkout: self
      submodules: recursive
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: 'public/msquic/qns'
        dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
        containerRegistry: '9196310f-afae-4a53-8e35-b9e753eeb0f3'
        ${{ if eq(variables['Build.Reason'], 'BatchedCI') }}:
          tags: |
            latest
            v1.1.0.$(Build.BuildId)
        ${{ if ne(variables['Build.Reason'], 'BatchedCI') }}:
          tags: custom-$(Build.BuildId)
- template: .\templates\run-qns.yml
  parameters:
    ${{ if parameters.build }}:
      dependsOn: publish_docker
    timeout: ${{ parameters.timeout }}
    measurements: ${{ parameters.measurements }}
    ${{ if and(parameters.build, ne(variables['Build.Reason'], 'BatchedCI')) }}:
      build: custom-$(Build.BuildId)
    ${{ if eq(parameters.client, 'all') }}:
      clients: [ 'quic-go', 'quicly', 'ngtcp2', 'quant', 'mvfst', 'quiche', 'kwik', 'picoquic', 'aioquic', 'neqo', 'msquic' ]
    ${{ if eq(parameters.client, 'special') }}:
      clients: [ 'quic-go', 'quant', 'msquic' ]
    ${{ if and(ne(parameters.client, 'special'), ne(parameters.client, 'all')) }}:
      clients: [ '${{ parameters.client }}' ]
    ${{ if eq(parameters.server, 'all') }}:
      servers: [ 'quic-go', 'quicly', 'ngtcp2', 'quant', 'mvfst', 'quiche', 'picoquic', 'aioquic', 'neqo', 'nginx', 'msquic' ]
    ${{ if ne(parameters.server, 'special') }}:
      servers: [ 'quic-go', 'quant', 'msquic' ]
    ${{ if and(ne(parameters.server, 'special'), ne(parameters.server, 'all')) }}:
      clients: [ '${{ parameters.server }}' ]
