#
# This pipeline executes the interop runner for all implementations.
#

trigger: none
pr: none

name: 0.$(Date:yyyy).$(Date:MM).$(DayOfMonth).$(Rev:rr).0

jobs:
- template: .\templates\run-qns.yml
  parameters:
    clients: [ 'quic-go', 'quicly', 'quant' ]
    servers: [ 'quic-go', 'quicly', 'quant', 'msquic' ]
