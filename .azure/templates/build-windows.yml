#
# Builds all Windows user mode configurations.
#

jobs:

# Officially supported configurations.

- template: ./build-config-user.yml
  parameters:
    image: windows-latest
    platform: windows
    arch: x86
    tls: schannel

- template: ./build-config-user.yml
  parameters:
    image: windows-latest
    platform: windows
    arch: x64
    tls: schannel

# Build currently fails at linker stage.
#- template: ./build-config-user.yml
#  parameters:
#    image: windows-latest
#    platform: windows
#    arch: arm
#    tls: schannel

# Build currently fails at linker stage.
#- template: ./build-config-user.yml
#  parameters:
#    image: windows-latest
#    platform: windows
#    arch: arm64
#    tls: schannel

# Other configurations.

- template: ./build-config-user.yml
  parameters:
    image: windows-latest
    platform: windows
    arch: x64
    tls: stub

- template: ./build-config-user.yml
  parameters:
    image: windows-latest
    platform: windows
    arch: x64
    tls: mitls
