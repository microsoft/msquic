#
# Builds all Windows kernel mode configurations.
#

jobs:

- template: ./build-config-winkernel.yml
  parameters:
    arch: x64

- template: ./build-config-winkernel.yml
  parameters:
    arch: x86

- template: ./build-config-winkernel.yml
  parameters:
    arch: arm

- template: ./build-config-winkernel.yml
  parameters:
    arch: arm64
