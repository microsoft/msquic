trigger: none # https://aka.ms/obpipelines/triggers

parameters: # parameters are shown up in ADO UI in a build queue time
- name: 'debug'
  displayName: 'Enable debug output'
  type: boolean
  default: false

variables:
  CDP_DEFINITION_BUILD_COUNT: $[counter('', 0)] # needed for onebranch.pipeline.version task https://aka.ms/obpipelines/versioning
  system.debug: ${{ parameters.debug }}
  ENABLE_PRS_DELAYSIGN: 1
  DisableDockerDetector: true
  ROOT: $(Build.SourcesDirectory)
  REPOROOT: $(Build.SourcesDirectory)
  OUTPUTROOT: $(REPOROOT)\out
  NUGET_XMLDOC_MODE: none

resources:
  repositories:
    - repository: templates
      type: git
      name: OneBranch.Pipelines/GovernedTemplates
      ref: refs/heads/main

extends:
  template: v2/OneBranch.Official.CrossPlat.yml@templates # https://aka.ms/obpipelines/templates
  parameters:
    featureFlags:
      skipPoliciesValidation: true
    globalSdl: # https://aka.ms/obpipelines/sdl
      tsa:
        enabled: false # onebranch publish all sdl results to TSA. If TSA is disabled all SDL tools will forced into 'break' build mode.

    stages:
      - stage: docker
        jobs:
        - job: linuxContainers # build linux images
          variables:
            ob_git_checkout: true
          pool:
            type: docker
            os: linux
          steps:
            - task: onebranch.pipeline.imagebuildinfo@1
              inputs:
                repositoryName: msquicbuild
                dockerFileRelPath: .azure/dockers/ob/Dockerfile
                dockerFileContextPath: .azure/dockers/ob
                saveImageToPath: msquicbuild.tar
                enable_network: true
                build_tag: $(Build.BuildNumber)
                enable_isolated_acr_push: true
                enable_service_tree_acr_path: true
